<h1>All Song</h1>

<ul>
    <% @songs.each do |song| %>
        <li> <a href="songs/<%= song.to_slug %>.html"><%= song.artist.name + " - " + song.name + " - " + song.genre.name%></a>/li>
    <% end %>
</ul>